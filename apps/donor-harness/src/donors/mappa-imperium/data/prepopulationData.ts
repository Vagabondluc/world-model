export const prepopulationData: Record<number, Record<number, {
    elements?: any[];
    elementUpdates?: any[];
    playerUpdates?: Record<string, any>;
} | undefined> | undefined> = {
    // Placeholder data - replace with actual prepopulation scenarios
};
